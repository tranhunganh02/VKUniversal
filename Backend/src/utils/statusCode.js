module.exports= {

/**
 * Mã trạng thái HTTP: Tiếp tục
 */
     CONTINUE: 100,

/**
 * Mã trạng thái HTTP: Chuyển đổi giao thức
 */
     SWITCHING_PROTOCOLS: 101,

/**
 * Mã trạng thái HTTP: Xử lý
 */
     PROCESSING: 102,

/**
 * Mã trạng thái HTTP: Gợi ý sớm
 */
     EARLY_HINTS: 103,

/**
 * Mã trạng thái HTTP: OK
 */
     OK: 200,

/**
 * Mã trạng thái HTTP: Đã tạo
 */
     CREATED: 201,

/**
 * Mã trạng thái HTTP: Đã chấp nhận
 */
     ACCEPTED: 202,

/**
 * Mã trạng thái HTTP: Không có nội dung
 */
     NO_CONTENT: 204,

/**
 * Mã trạng thái HTTP: Đặt lại nội dung
 */
     RESET_CONTENT: 205,

/**
 * Mã trạng thái HTTP: Nội dung một phần
 */
     PARTIAL_CONTENT: 206,

/**
 * Mã trạng thái HTTP: Đa trạng thái (WebDAV)
 */
     MULTI_STATUS: 207,

/**
 * Mã trạng thái HTTP: Đã báo cáo
 */
     ALREADY_REPORTED: 208,

/**
 * Mã trạng thái HTTP: Đã sử dụng IM
 */
     IM_USED: 226,

/**
 * Mã trạng thái HTTP: Lựa chọn nhiều
 */
     MULTIPLE_CHOICES: 300,

/**
 * Mã trạng thái HTTP: Di chuyển vĩnh viễn
 */
     MOVED_PERMANENTLY: 301,

/**
 * Mã trạng thái HTTP: Tìm thấy
 */
     FOUND: 302,

/**
 * Mã trạng thái HTTP: Xem khác
 */
     SEE_OTHER: 303,

/**
 * Mã trạng thái HTTP: Không sửa đổi
 */
     NOT_MODIFIED: 304,

/**
 * Mã trạng thái HTTP: Sử dụng proxy
 */
     USE_PROXY: 305,

/**
 * Mã trạng thái HTTP: Chuyển hướng tạm thời
 */
     TEMPORARY_REDIRECT: 307,

/**
 * Mã trạng thái HTTP: Chuyển hướng vĩnh viễn
 */
     PERMANENT_REDIRECT: 308,

/**
 * Mã trạng thái HTTP: Yêu cầu sai
 */
     BAD_REQUEST: 400,

/**
 * Mã trạng thái HTTP: Chưa được ủy quyền
 */
     UNAUTHORIZED: 401,

/**
 * Mã trạng thái HTTP: Yêu cầu thanh toán
 */
     PAYMENT_REQUIRED: 402,

/**
 * Mã trạng thái HTTP: Bị cấm
 */
     FORBIDDEN: 403,

/**
 * Mã trạng thái HTTP: Không tìm thấy
 */
     NOT_FOUND: 404,

/**
 * Mã trạng thái HTTP: Phương thức không được phép
 */
     METHOD_NOT_ALLOWED: 405,

/**
 * Mã trạng thái HTTP: Không thể chấp nhận
 */
     NOT_ACCEPTABLE: 406,

/**
 * Mã trạng thái HTTP: Yêu cầu xác thực proxy
 */
     PROXY_AUTHENTICATION_REQUIRED: 407,

/**
 * Mã trạng thái HTTP: Hết thời gian chờ yêu cầu
 */
     REQUEST_TIMEOUT: 408,

/**
 * Mã trạng thái HTTP: Xung đột
 */
     CONFLICT: 409,

/**
 * Mã trạng thái HTTP: Đã đi
 */
     GONE: 410,

/**
 * Mã trạng thái HTTP: Cần độ dài
 */
     LENGTH_REQUIRED: 411,

/**
 * Mã trạng thái HTTP: Điều kiện thất bại
 */
     PRECONDITION_FAILED: 412,

/**
 * Mã trạng thái HTTP: Tải trọng quá lớn
 */
     PAYLOAD_TOO_LARGE: 413,

/**
 * Mã trạng thái HTTP: URI quá dài
 */
     URI_TOO_LONG: 414,

/**
 * Mã trạng thái HTTP: Loại phương tiện không được
*/
}